<h1><%= @article.title %></h1>

<p><%= @article.content %></p>


  <%= link_to 'back', articles_path %>
  <%= link_to 'edit', article_edit_path(@article) %>
  <%= link_to 'delete', article_path(@article), method: :delete, data: { confirm: "Are you sure to deliete #{@article.title}? "} %>
